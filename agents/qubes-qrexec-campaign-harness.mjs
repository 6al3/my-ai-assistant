import { spawn } from 'node:child_process';
import { randomUUID } from 'node:crypto';
import { pathToFileURL } from 'node:url';
import { setTimeout as sleep } from 'node:timers/promises';
import { signWorkerEnvelope } from './worker-transport-envelope.mjs';
import { verifyCoordinatorResponseAttestation } from './qrexec-response-attestation.mjs';
function assertString(value,name){if(typeof value!=='string'||value.trim()==='')throw new TypeError(`${name} must be a non-empty string`);return value.trim();}
function assertRunId(value,name='runId'){const runId=assertString(value,name);if(runId.length>128||!/^[A-Za-z0-9._-]+$/.test(runId))throw new Error(`${name} must be 1-128 characters using letters, digits, dot, underscore, or hyphen`);return runId;}
function assertResponse(value,name='response'){if(!value||typeof value!=='object'||Array.isArray(value)||typeof value.ok!=='boolean')throw new Error(`${name} must be a coordinator response object`);return value;}
export function verifyQrexecCoordinatorResponse(response,{publicKeyPem,expectedKeyId,expectedGitSha,expectedService,expectedRequestId}={}){return verifyCoordinatorResponseAttestation(assertResponse(response),{publicKeyPem:assertString(publicKeyPem,'publicKeyPem'),expectedKeyId:assertString(expectedKeyId,'expectedKeyId'),expectedGitSha:assertString(expectedGitSha,'expectedGitSha'),expectedService:assertString(expectedService,'expectedService'),expectedRequestId:assertString(expectedRequestId,'expectedRequestId')});}
function countRunTokens(value){if(typeof value==='string')return value.split('{{RUN_ID}}').length-1;if(Array.isArray(value))return value.reduce((s,i)=>s+countRunTokens(i),0);if(value&&typeof value==='object')return Object.values(value).reduce((s,i)=>s+countRunTokens(i),0);return 0;}
function replaceRunTokens(value,runId){if(typeof value==='string')return value.replaceAll('{{RUN_ID}}',runId);if(Array.isArray(value))return value.map(i=>replaceRunTokens(i,runId));if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value).map(([k,i])=>[k,replaceRunTokens(i,runId)]));return value;}
export function materializeQrexecCampaignSteps({steps,runId,requireRunToken=false}={}){if(!Array.isArray(steps)||steps.length===0)throw new TypeError('steps must be a non-empty array');const id=assertRunId(runId);if(requireRunToken&&countRunTokens(steps)===0)throw new Error('campaign must contain at least one {{RUN_ID}} token');return replaceRunTokens(steps,id);}
function resolveRef(value,captures,name='value'){if(Array.isArray(value))return value.map((i,n)=>resolveRef(i,captures,`${name}[${n}]`));if(!value||typeof value!=='object')return value;if(Object.keys(value).length===1&&typeof value.$ref==='string'){const[captureName,...path]=value.$ref.split('.');let current=captures.get(captureName);if(current===undefined)throw new Error(`${name} references unknown capture: ${captureName}`);for(const segment of path){if(current==null||typeof current!=='object'||!(segment in current))throw new Error(`${name} references missing path: ${value.$ref}`);current=current[segment];}return structuredClone(current);}return Object.fromEntries(Object.entries(value).map(([k,i])=>[k,resolveRef(i,captures,`${name}.${k}`)]));}
function missionGraphEvidence(response,name='submit response'){const missions=response?.result?.missions;if(!Array.isArray(missions)||missions.length===0)throw new Error(`${name} must include non-empty result.missions`);const missionIds=missions.map((mission,index)=>assertString(mission?.id,`${name}.result.missions[${index}].id`)),missionSet=new Set(missionIds);if(missionSet.size!==missionIds.length)throw new Error(`${name} contains duplicate mission ids`);const dependencyEdges=[];for(const[missionIndex,mission]of missions.entries()){const dependsOn=mission?.dependsOn??[];if(!Array.isArray(dependsOn))throw new Error(`${name}.result.missions[${missionIndex}].dependsOn must be an array`);for(const[dependencyIndex,dependency]of dependsOn.entries()){const from=assertString(dependency,`${name}.result.missions[${missionIndex}].dependsOn[${dependencyIndex}]`),to=missionIds[missionIndex];if(!missionSet.has(from))throw new Error(`${name} dependency ${from} is outside submitted mission graph`);dependencyEdges.push({from,to});}}return{type:'mission_graph_instance',missionIds,dependencyEdges};}
export function createQrexecProcessTransport({target,service,qrexecBin='qrexec-client-vm',env=process.env,now=()=>Date.now(),attestation=null}={}){target=assertString(target,'target');service=assertString(service,'service');qrexecBin=assertString(qrexecBin,'qrexecBin');return async function invoke(envelope,{service:serviceOverride=service}={}){const selectedService=assertString(serviceOverride,'service'),requestId=assertString(envelope?.requestId,'envelope.requestId'),startedAt=now();const child=spawn(qrexecBin,[target,selectedService],{env,stdio:['pipe','pipe','pipe']});let stdout='',stderr='';child.stdout.setEncoding('utf8');child.stderr.setEncoding('utf8');child.stdout.on('data',c=>stdout+=c);child.stderr.on('data',c=>stderr+=c);child.stdin.on('error',()=>{});child.stdin.end(`${JSON.stringify(envelope)}\n`);const exit=await new Promise((resolve,reject)=>{child.once('error',reject);child.once('close',(code,signal)=>resolve({code,signal}));});const durationMs=Math.max(0,now()-startedAt),lines=stdout.split(/\r?\n/).map(l=>l.trim()).filter(Boolean);if(exit.code!==0||lines.length!==1){const detail=stderr.trim()||`exit=${exit.code} signal=${exit.signal??'none'} responses=${lines.length}`;const error=new Error(`qrexec transport failed: ${detail}`);error.durationMs=durationMs;throw error;}let response;try{response=JSON.parse(lines[0]);}catch(error){throw new Error(`qrexec transport returned invalid JSON: ${error instanceof Error?error.message:String(error)}`);}response=assertResponse(response);let attestationVerified=null;if(attestation){response=verifyQrexecCoordinatorResponse(response,{...attestation,expectedService:selectedService,expectedRequestId:requestId});attestationVerified={service:selectedService,keyId:assertString(attestation.expectedKeyId,'attestation.expectedKeyId'),gitSha:assertString(attestation.expectedGitSha,'attestation.expectedGitSha'),requestId};}return{response,durationMs,attestationVerified};};}
function recordVerifiedAttestation(events,outcome,requestId){if(!outcome?.attestationVerified)return;const e=outcome.attestationVerified;if(assertString(e.requestId,'attestationVerified.requestId')!==requestId)throw new Error(`verified attestation requestId mismatch for ${requestId}`);events.push({type:'attestation_verified',service:assertString(e.service,'attestationVerified.service'),keyId:assertString(e.keyId,'attestationVerified.keyId'),gitSha:assertString(e.gitSha,'attestationVerified.gitSha'),requestId});}
export async function runQrexecCampaignSteps({steps,invoke,secret,issuedAt=()=>Date.now(),sleepFn=sleep}={}){if(!Array.isArray(steps)||steps.length===0)throw new TypeError('steps must be a non-empty array');if(typeof invoke!=='function')throw new TypeError('invoke must be a function');if(typeof sleepFn!=='function')throw new TypeError('sleepFn must be a function');const events=[],captures=new Map();const send=async(step,service)=>{const body=resolveRef(step.body??null,captures,'step.body');return invoke(signWorkerEnvelope({requestId:assertString(step.requestId,'step.requestId'),issuedAt:issuedAt(),op:assertString(step.op,'step.op'),body,secret}),{service});};for(const[index,rawStep]of steps.entries()){if(!rawStep||typeof rawStep!=='object'||Array.isArray(rawStep))throw new TypeError(`step[${index}] must be an object`);const step=rawStep,mode=step.mode??'request';if(mode==='wait'){const durationMs=Number(step.durationMs);if(!Number.isFinite(durationMs)||durationMs<0||durationMs>120000)throw new Error(`step[${index}].durationMs must be between 0 and 120000`);await sleepFn(durationMs);events.push({type:'wait',durationMs});continue;}const requestId=assertString(step.requestId,`step[${index}].requestId`);events.push({type:'request_pending',requestId});if(mode==='crash_retry'){let firstFailed=false;try{await send(step,assertString(step.faultService,`step[${index}].faultService`));}catch{firstFailed=true;}if(!firstFailed)throw new Error(`step[${index}] expected the fault service to terminate without a committed response`);const recovered=await send(step,assertString(step.recoveryService,`step[${index}].recoveryService`));recordVerifiedAttestation(events,recovered,requestId);assertResponse(recovered.response,`step[${index}] recovery response`);if(!recovered.response.ok)throw new Error(`step[${index}] recovery failed: ${recovered.response.error??'unknown error'}`);if(step.saveAs)captures.set(assertString(step.saveAs,`step[${index}].saveAs`),structuredClone(recovered.response));events.push({type:'recovery',durationMs:recovered.durationMs},{type:'round_trip',durationMs:recovered.durationMs},{type:'request_resolved',requestId});if(step.mutationKey)events.push({type:'mutation_committed',mutationKey:assertString(step.mutationKey,`step[${index}].mutationKey`)});continue;}const outcome=await send(step,step.service);recordVerifiedAttestation(events,outcome,requestId);const response=assertResponse(outcome.response,`step[${index}] response`);if(step.saveAs)captures.set(assertString(step.saveAs,`step[${index}].saveAs`),structuredClone(response));events.push({type:'round_trip',durationMs:outcome.durationMs});if(mode==='stale_probe'){events.push({type:'stale_completion_probe',rejected:!response.ok});if(response.ok)events.push({type:'stale_completion'});events.push({type:'request_resolved',requestId});continue;}if(mode==='qa_barrier_probe'){if(!response.ok)throw new Error(`step[${index}] QA barrier probe failed: ${response.error??'unknown error'}`);events.push({type:'qa_barrier_probe',blocked:response.result==null},{type:'request_resolved',requestId});continue;}if(mode==='qa_post_join_probe'){if(!response.ok)throw new Error(`step[${index}] QA post-join probe failed: ${response.error??'unknown error'}`);if(response.result==null)throw new Error(`step[${index}] expected QA mission after dependency join`);if(step.saveAs)captures.set(assertString(step.saveAs,`step[${index}].saveAs`),structuredClone(response));events.push({type:'qa_started',pendingDependencies:0},{type:'qa_post_join_start'},{type:'request_resolved',requestId});continue;}if(mode!=='request')throw new Error(`unsupported campaign step mode: ${mode}`);const expectError=step.expectError===true;if(expectError?response.ok:!response.ok)throw new Error(`step[${index}] unexpected coordinator response: ${JSON.stringify(response)}`);if(!expectError&&step.op==='submit')events.push(missionGraphEvidence(response,`step[${index}] submit response`));events.push({type:'request_resolved',requestId});if(!expectError&&step.op==='submit'&&step.body?.options?.idempotencyKey)events.push({type:'enqueue_idempotency_identity',idempotencyKey:assertString(step.body.options.idempotencyKey,`step[${index}].body.options.idempotencyKey`)});if(!expectError&&step.mutationKey)events.push({type:'mutation_committed',mutationKey:assertString(step.mutationKey,`step[${index}].mutationKey`)});if(!expectError&&step.fencingCurrentCompletion===true)events.push({type:'current_lease_completion'});}return events;}
async function readStdin(){let input='';process.stdin.setEncoding('utf8');for await(const chunk of process.stdin)input+=chunk;return input;}
async function main(){const target=assertString(process.env.DIG_QREXEC_TARGET,'DIG_QREXEC_TARGET'),sourceQube=assertString(process.env.DIG_QREXEC_SOURCE,'DIG_QREXEC_SOURCE'),service=assertString(process.env.DIG_QREXEC_SERVICE,'DIG_QREXEC_SERVICE'),gitSha=assertString(process.env.DIG_GIT_SHA,'DIG_GIT_SHA');if(!/^[0-9a-f]{40}$/i.test(gitSha))throw new Error('DIG_GIT_SHA must be a 40-character hex SHA');if(sourceQube===target)throw new Error('DIG_QREXEC_SOURCE and DIG_QREXEC_TARGET must differ');const secret=process.env.DIG_TRANSPORT_SECRET;if(!secret||Buffer.byteLength(secret,'utf8')<32)throw new Error('DIG_TRANSPORT_SECRET must be at least 32 bytes');const publicKeyPem=assertString(process.env.DIG_RESPONSE_ATTESTATION_PUBLIC_KEY,'DIG_RESPONSE_ATTESTATION_PUBLIC_KEY'),expectedKeyId=assertString(process.env.DIG_RESPONSE_ATTESTATION_KEY_ID,'DIG_RESPONSE_ATTESTATION_KEY_ID'),parsed=JSON.parse(await readStdin()),rawSteps=Array.isArray(parsed)?parsed:parsed.steps,runId=assertRunId(process.env.DIG_CAMPAIGN_RUN_ID||randomUUID(),'DIG_CAMPAIGN_RUN_ID'),steps=materializeQrexecCampaignSteps({steps:rawSteps,runId}),startedAt=new Date().toISOString(),rawInvoke=createQrexecProcessTransport({target,service,qrexecBin:process.env.DIG_QREXEC_BIN||'qrexec-client-vm',attestation:{publicKeyPem,expectedKeyId,expectedGitSha:gitSha}}),serviceCalls=[];const invoke=async(envelope,options={})=>{const selectedService=assertString(options.service??service,'qrexec service');serviceCalls.push(selectedService);const outcome=await rawInvoke(envelope,{...options,service:selectedService});if(!outcome.attestationVerified)throw new Error(`verified attestation evidence missing for ${selectedService}`);return outcome;};const events=await runQrexecCampaignSteps({steps,invoke,secret});process.stdout.write(`${JSON.stringify({type:'campaign_start',runId,transport:'qrexec',sourceQube,targetQube:target,service,gitSha,startedAt})}\n`);for(const selectedService of serviceCalls)process.stdout.write(`${JSON.stringify({type:'qrexec_service_call',service:selectedService})}\n`);for(const event of events)process.stdout.write(`${JSON.stringify(event)}\n`);process.stdout.write(`${JSON.stringify({type:'campaign_end',runId,finishedAt:new Date().toISOString()})}\n`);}
if(import.meta.url===pathToFileURL(process.argv[1]??'').href)main().catch(error=>{process.stderr.write(`DIG Qubes qrexec campaign harness failed: ${error instanceof Error?error.message:String(error)}\n`);process.exitCode=1;});