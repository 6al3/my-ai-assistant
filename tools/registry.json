{
  "version": 1,
  "description": "Catalog of available local tools. Tool entries are metadata; execution is handled by explicit integrations.",
  "tools": [
    {
      "id": "web-fetch",
      "name": "Public Web Fetch",
      "endpoint": "/api/web",
      "method": "GET",
      "capabilities": ["http", "https", "text", "json"],
      "limits": {
        "maxBytes": 2097152,
        "timeoutMs": 15000,
        "privateNetworks": false
      }
    }
  ]
}
